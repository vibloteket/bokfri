<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>
    <!-- CLI stdout and stderr are an API, especially for JSON output. Unexpected
         exceptions are written explicitly to bokfri-cli.log by the command handler. -->
    <root level="OFF"/>
</configuration>
